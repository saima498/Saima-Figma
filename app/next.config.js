// /** @type {import('next').NextConfig} */
 
// next.config.js
module.exports = {
    images: {
      domains: [
        'cdn.sanity.io', // Ensure Sanity domain is allowed
        'via.placeholder.com', 
        
      ],
    },
   
  };
